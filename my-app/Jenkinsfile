pipeline {
    agent {
      label 'linux'
    }
    tools {
        jdk 'oracle-7u80'
    }
    stages {
    stage('maven install') {
      steps {
        withMaven(maven: 'Maven3') {
      sh 'mvn clean install'
  }
      }
    }

  }

  
}
